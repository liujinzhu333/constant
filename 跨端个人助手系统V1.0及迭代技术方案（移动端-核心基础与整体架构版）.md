# 跨端个人助手系统V1\.0及迭代技术方案（移动端\-核心基础与整体架构版）

# 一、文档分批次实现说明

## 1\.1 实现逻辑

为保障技术文档的完整性、逻辑连贯性，本次文档聚焦移动端核心基础与整体架构设计，采用聚焦核心、简化冗余的实现思路，明确移动端系统的核心定位、整体架构及基础技术规范，确保内容聚焦移动端场景、可落地、可迭代。

本次技术方案的核心架构为“基座\+业务包热更新”，所有可迭代、可优化的内容均围绕移动端业务包设计；基座仅负责移动端基础运行支撑，相关内容明确后固定不变，后续不做修改，以此保障技术方案的一致性与可落地性，适配移动端轻量化、离线运行的核心需求。

## 1\.2 批次划分说明（仅聚焦核心批次）

### 核心批次：核心基础与整体架构（聚焦移动端）

本批次是移动端系统技术方案的底层基础，核心目的是明确移动端系统的核心定位、整体架构及基础技术规范，具体覆盖内容包括：移动端系统需求概述、整体架构选型、移动端前后端技术栈选型、移动端系统模块划分（基座与业务包分层）、移动端热更新核心设计及移动端适配细节，为后续移动端业务模块设计提供明确依据，适配Android、iOS双系统运行需求。

# 二、移动端整体架构设计（基座\+业务包架构）

## 2\.1 架构选型

结合移动端系统核心需求（独立离线运行、轻量化、快速热更新迭代、多机型适配），本次移动端系统采用“独立离线架构\+基座\+业务包热更新”的核心架构模式，彻底摒弃单机服务器依赖，无需用户搭建内网环境或启动额外服务器，降低用户使用门槛，适配移动端随时随地使用的场景。

移动端采用“基座\+业务包”分层设计，清晰划分两层职责边界：基座层负责提供移动端基础运行支撑，不包含任何业务逻辑；业务包层负责封装移动端所有业务功能，是后续迭代更新的核心载体。移动端无需跨端同步（仅保留单端离线运行），由基座层提供本地运行基础能力，业务包层负责所有业务逻辑实现，确保热更新迭代不影响移动端运行稳定性，同时控制应用体积，适配移动端存储与性能需求。

## 2\.2 技术栈选型（仅移动端）

### 2\.2\.1 前端技术栈（移动端适配\+热更新优先）

前端技术栈选型核心遵循“移动端兼容、轻量化、支持热更新、多机型适配”原则，聚焦Android9及以上、iOS13及以上系统适配，兼顾手机、平板等不同设备尺寸，具体选型如下表所示：

|平台|核心技术（基座）|核心技术（业务包）|辅助技术（热更新相关）|说明|
|---|---|---|---|---|
|移动端（Android/iOS）|Vue3 \+ Vite \+ UniApp \+ 轻量Node\.js运行时（v16\+）|Vue3 \+ Vite \+ uView \+ Pinia|uni\-app热更新插件、沙盒文件管理、增量更新工具|基座负责移动端应用启动、热更新引擎运行、系统适配；业务包封装所有移动端业务功能，支持热更新；SQLite3数据库由基座提供基础连接，业务包负责具体数据操作，控制应用体积。|
|公共依赖|SQLite3、Axios（备用）、Day\.js|Vue Router、本地接口封装、移动端数据处理工具|版本校验、增量更新算法（bsdiff）、更新日志管理、机型适配工具|基座提供公共依赖支撑，业务包直接复用；热更新采用差异同步，减少移动端更新体积，节省流量与存储，适配移动端网络场景。|

### 2\.2\.2 后端技术栈（适配移动端业务包热更新）

移动端系统无独立后端服务器，后端逻辑基于移动端内置的嵌入式运行环境实现，核心适配“基座\+业务包”架构，基座提供基础运行环境，业务包实现具体业务逻辑，兼顾移动端轻量化、低功耗需求，具体选型如下：

|模块|核心技术|归属层（基座/业务包）|说明|
|---|---|---|---|
|开发语言/框架|Node\.js（轻量嵌入式版本，v16\+）|基座|嵌入式集成于移动端基座，提供本地业务逻辑运行环境，不包含具体业务代码，适配Android、iOS双系统，兼顾低功耗需求。|
|本地数据库|SQLite3（移动端本地部署，v3\.40\+）|基座（基础连接）、业务包（数据操作）|基座负责移动端数据库初始化，业务包负责数据增删改查等操作，表结构调整通过业务包热更新实现，不影响基座运行，控制数据库占用存储。|
|本地存储|移动端沙盒文件存储 \+ SQLite3|基座（存储基础）、业务包（文件操作）|基座负责移动端沙盒路径与权限管理，遵循Android、iOS存储权限规范，业务包负责静态资源读写，热更新不影响已存储资源，节省移动端存储。|
|推送服务|移动端系统通知（Android通知、iOS通知）|基座（推送基础）、业务包（提醒逻辑）|基座适配移动端系统通知机制，申请对应权限，业务包实现提醒触发逻辑，可通过热更新迭代，适配移动端消息推送规范。|
|热更新引擎|uni\-app热更新插件（移动端专用）|基座|基座内置热更新引擎，负责移动端业务包版本检测、下载、安装与加载，固定不变，适配Android、iOS双系统热更新场景，支持流量控制。|

### 2\.2\.3 部署与运维（适配移动端热更新架构）

部署与运维核心适配移动端“基座\+业务包”热更新架构，摒弃复杂的服务器部署与监控依赖，聚焦移动端应用部署、业务包更新管理及多机型适配，具体规划如下：

- 容器化：采用“基座\+业务包”分离打包部署模式，移动端基座仅需打包一次，上传至应用商店（Android应用市场、App Store），后续功能迭代仅需更新业务包，无需重新提交应用商店审核，适配移动端应用发布规范。

- 服务器：取消单机服务器依赖，仅部署轻量化业务包更新服务器（云服务器轻量化部署），用于存储移动端业务包更新文件，支持用户联网自动更新（可设置Wi\-Fi优先）、离线手动导入更新。

- CI/CD：采用Jenkins构建自动化部署流程，实现移动端基座一次性打包、业务包自动化构建、版本管理及更新包上传，适配移动端业务包热更新需求，缩短迭代周期。

- 监控：移动端内置本地日志记录功能，记录基座运行日志、业务包运行日志、热更新日志，按天轮转保留30天，占用少量存储，便于本地问题排查，无需额外监控工具，适配移动端低功耗需求。

## 2\.3 移动端系统模块划分

### 2\.3\.1 基座模块（移动端专用，固定不变）

移动端基座模块采用低耦合、轻量化设计，仅提供移动端基础支撑能力，不包含任何业务逻辑，后续不做修改，兼顾移动端性能与功耗，核心模块如下：

1. 应用启动模块：负责移动端应用启动初始化、基座核心组件加载、业务包精准加载，确保移动端应用启动时间控制在3秒内，适配不同配置的移动设备，降低启动功耗。

2. 热更新模块：负责移动端业务包版本检测、更新包下载、完整性校验、旧包卸载、新包安装与加载，支持自动与手动更新，可设置Wi\-Fi优先更新，不中断移动端应用运行，节省流量。

3. 运行环境模块：提供移动端嵌入式Node\.js运行环境、前端渲染基础组件，为业务包运行提供底层支撑，适配Android9及以上、iOS13及以上系统，兼顾低功耗。

4. 本地存储基础模块：负责移动端SQLite3数据库初始化、沙盒存储路径管理、权限控制，支持本地数据AES\-256加密存储，遵循Android、iOS存储权限规范，保障数据安全。

5. 系统适配模块：适配移动端双系统（Android、iOS），负责屏幕适配、手势操作、系统通知、权限申请等适配，保障移动端基座稳定运行，兼容不同机型、不同屏幕尺寸。

6. 日志模块：负责移动端基座运行日志的记录、存储与管理，为基座底层问题排查提供支撑，控制日志存储体积，适配移动端存储需求。

### 2\.3\.2 业务包模块（移动端专用，可热更新，后续迭代核心）

移动端业务包模块封装所有移动端业务功能，模块间低耦合、高内聚，便于热更新迭代，同时控制模块体积，适配移动端存储与性能需求，核心模块如下：

1. 账号与本地数据管理模块：负责移动端本地账号登录、权限校验、本地数据管理及数据备份与恢复，内置热更新数据兼容逻辑，适配移动端快捷登录场景。

2. 待办任务模块：负责任务全生命周期管理（增删改查、状态切换、提醒等），封装移动端前端界面与业务逻辑，支持热更新迭代，适配移动端手势操作习惯。

3. 学习计划模块：负责学习计划创建、任务拆解、进度统计、节点提醒等，核心逻辑可通过热更新优化，适配移动端碎片化学习场景。

4. 笔记与日程模块：负责轻量笔记管理、日程视图展示与管理，移动端界面与逻辑可通过热更新调整，适配移动端小屏操作习惯，支持快捷编辑。

5. 提醒中心模块：负责移动端全局提醒统一管理、提醒触发与推送，提醒逻辑可通过热更新迭代，适配移动端系统通知机制，支持震动、铃声等提醒方式。

6. 业务适配模块：负责与移动端基座的交互适配，确保业务包正常加载、热更新后与基座无缝衔接，适配Android、iOS双系统运行，兼容不同机型。

7. 数据兼容模块：负责移动端业务包版本更新时的数据兼容与迁移，确保热更新不丢失用户数据，适配移动端数据存储特点。

## 2\.4 移动端热更新核心设计

### 2\.4\.1 热更新范围（移动端专用）

后续移动端所有可迭代、可优化内容均封装于业务包，支持热更新，基座模块固定不变，具体更新范围如下，兼顾移动端体积控制：

- 功能模块：新增、优化移动端待办、学习计划、笔记等所有业务功能，扩展移动端效率工具等功能，控制功能模块体积。

- 业务逻辑：优化移动端数据处理、提醒触发等所有业务逻辑，提升运行效率与稳定性，降低移动端功耗，适配移动端运行环境。

- 界面渲染：调整移动端所有业务界面的布局、样式、交互效果，优化移动端小屏操作体验，适配不同屏幕尺寸与机型。

- 本地接口：更新移动端本地接口实现逻辑、入参/出参格式，优化错误码设计，适配移动端业务需求与性能要求。

- 数据处理：调整移动端数据库表结构、数据清理规则，通过业务包内置脚本实现，不破坏原有数据，控制数据库体积。

### 2\.4\.2 热更新机制（移动端专用）

移动端热更新采用“自动\+手动”双触发方式，结合增量更新与全量更新策略，兼顾流量节省、存储控制与更新稳定性，适配移动端网络场景与设备特点，具体机制如下：

- 更新触发：自动触发（移动端应用启动、后台运行时，联网状态下自动检测更新，可设置Wi\-Fi优先更新，避免消耗移动流量）；手动触发（用户在移动端应用设置中手动检查更新）；离线状态支持手动导入更新包升级。

- 更新方式：小版本迭代（如V1\.0→V1\.0\.1）采用增量更新，仅下载业务包差异内容，减少更新体积，节省流量与存储；大版本迭代（如V1\.0→V1\.1）采用全量更新，确保业务包完整性。

- 更新流程：版本检测→更新包下载（支持断点续传）→MD5完整性校验→旧包卸载→新包安装→新包加载→更新完成，无需重启移动端应用，不中断用户操作，适配移动端碎片化使用场景。

- 回滚机制：更新异常（校验失败、加载失败）时自动回滚至旧版本；支持用户手动回滚，应对移动端更新后功能异常、卡顿等场景，保障用户体验。

### 2\.4\.3 热更新兼容性保障（移动端专用）

为确保移动端热更新过程稳定、数据安全，适配不同机型与系统版本，重点做好以下兼容性保障：

- 版本兼容：移动端业务包版本与基座版本绑定，业务包向下兼容，旧版本业务包可正常加载到基座；新增版本需兼容移动端历史数据，同时适配不同系统版本（Android9\+、iOS13\+）。

- 数据安全：热更新不修改移动端用户本地数据，仅更新业务包逻辑与界面；表结构调整前先备份数据，通过兼容脚本实现，避免数据丢失，适配移动端数据存储特点。

- 运行稳定：更新前检测移动端业务包与基座兼容性、与当前系统版本兼容性，不兼容则禁止更新；更新后检测加载状态，失败则自动回滚，避免移动端应用崩溃、卡顿，适配不同配置的移动设备。

## 2\.5 移动端适配细节

适配Android9及以上、iOS13及以上系统，聚焦移动端操作体验、多机型适配、低功耗与存储控制，具体细节如下：

- 界面适配：适配不同屏幕尺寸（手机、平板），支持竖屏/横屏自动切换，界面布局适配小屏操作，优化手势交互（如滑动切换、下拉刷新），适配不同机型的刘海屏、全面屏，确保界面显示完整。

- 存储适配：采用系统沙盒路径存储，仅应用可访问，遵循Android、iOS存储权限规范，控制应用整体体积（基座\+初始业务包≤100MB），定期清理冗余数据，节省移动端存储。

- 热更新适配：通过uni\-app热更新插件实现，支持断点续传、Wi\-Fi优先更新，更新包体积控制在50MB以内，适配移动端网络场景（Wi\-Fi/移动数据），不干扰用户操作。

- 运行适配：优化移动端运行性能，确保应用启动时间≤3秒，无卡顿、闪退现象，降低运行功耗，适配不同配置的移动设备（低配置设备也能稳定运行），兼容Android、iOS双系统操作习惯。

- 权限适配：严格遵循移动端权限规范，仅申请必要权限（存储、通知、网络），权限申请有明确提示，用户可自主开启/关闭，保障用户隐私安全。

\-\-end\-\-

> （注：文档部分内容可能由 AI 生成）
